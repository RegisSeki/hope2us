<header id="master">
  <%= render 'shared/nav_default' unless user_signed_in? || admin_user_signed_in? %>
  <%= render 'shared/nav_user' if user_signed_in? %>
  <%= render 'shared/nav_admin_user' if admin_user_signed_in? %>
</header>
